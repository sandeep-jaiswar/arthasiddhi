# Environment variables for the NestJS Microservice

# Original HTTP port (if still needed or for reference)
HTTP_PORT=3000

# TCP Transport
TCP_HOST=localhost
TCP_PORT=3001

# Redis Transport
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_URL=redis://localhost:6379 # Alternative using URL

# gRPC Transport
GRPC_PACKAGE=hero # TODO: Replace with your gRPC package name(s)
GRPC_PROTO_PATH=_proto/hero.proto # TODO: Replace with relative path to your .proto file(s)
GRPC_URL=localhost:5000 # TODO: Replace with your gRPC server address and port

# Kafka Transport
KAFKA_CLIENT_ID=my-kafka-client # TODO: Replace with a unique client ID
KAFKA_BROKERS=localhost:9092 # TODO: Replace with your Kafka broker address(es) (comma-separated if multiple)
KAFKA_CONSUMER_GROUP_ID=my-kafka-group # TODO: Replace with a unique consumer group ID

# Add any other environment variables here...
